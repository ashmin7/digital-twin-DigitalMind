// This TypeScript file is kept minimal to satisfy Next.js type checking.
// The actual Tailwind configuration used by the build is defined in
// `tailwind.config.mjs`, which uses plain JavaScript and avoids TypeScript
// type dependencies.

export default {};
